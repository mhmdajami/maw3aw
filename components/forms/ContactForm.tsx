"use client"; import { FormEvent,useState } from "react";
export function ContactForm(){const [done,setDone]=useState(false);function submit(e:FormEvent){e.preventDefault();setDone(true)}if(done)return <div role="status" className="rounded-[24px] bg-[#F5F0E8] p-10 text-center"><span className="text-4xl">✓</span><h2 className="mt-4 text-4xl">Message received.</h2><p className="mt-3 text-sm opacity-60">We’ll get back to you as soon as the humans are available.</p></div>;return <form onSubmit={submit} className="grid gap-5 md:grid-cols-2"><Label text="Name"><input className="field" required name="name"/></Label><Label text="Email"><input className="field" required type="email" name="email"/></Label><Label text="Phone"><input className="field" type="tel" name="phone"/></Label><Label text="Subject"><input className="field" required name="subject"/></Label><Label text="Message" wide><textarea className="field min-h-36 resize-y" required name="message"/></Label><div className="md:col-span-2"><button className="btn btn-dark px-8">Send message</button></div></form>}
function Label({text,children,wide}:{text:string;children:React.ReactNode;wide?:boolean}){return <label className={wide?"md:col-span-2":""}><span className="mb-2 block text-xs font-bold uppercase tracking-wider">{text}</span>{children}</label>}
